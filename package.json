{
  "name": "linkedin-auto-share",
  "version": "1.0.0",
  "description": "LinkedIn automatic post sharing automation using Gemini AI and Serper news",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "share:now": "ts-node src/index.ts --once",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "node-cron": "^3.0.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-cron": "^3.0.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
